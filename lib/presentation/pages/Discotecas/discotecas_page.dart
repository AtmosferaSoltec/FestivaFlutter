import 'package:flutter/material.dart';

class DiscotecasPage extends StatelessWidget {
  const DiscotecasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Discotecas'),
      ),
    );
  }
}
